# ============================================================
# 模型凭据。复制成 .env 再填：cp .env.example .env
#
# 用哪一家由 config.yaml 的 greeting.provider 决定（anthropic / openai），
# 本文件只管把对应那家的凭据给到 SDK。
#
# 同一家的几个变量算「一组」：只要本文件声明了某组里的任意一个，
# 同组里没声明的就会从继承的 shell 环境里清掉（见 cli.py 的 _apply_env_file）。
# 这样能避免 .env 的网关地址配上了、shell 里的 key 却抢先生效导致认证失败。
# 两组之间互不影响。被注释掉的行不算声明。
# ============================================================

# ============ OpenAI（config.yaml 里 provider: openai 时用这组）============
# 注意：provider: openai 只是「说 OpenAI 那套协议」，不代表用 OpenAI 的模型。
# 任何 /v1/chat/completions 的兼容实现都走这一档（各类中转、DeepSeek、Kimi、通义…）。

# —— 方式一：官方 API ——
OPENAI_API_KEY=sk-your-key-here

# —— 方式二：OpenAI 兼容的第三方网关 / 国内中转 ——
# 填了 BASE_URL 就用网关，API_KEY 换成网关发的那个 key。
#
# 两个必踩的坑：
# 1. 末尾的 /v1 不能省。省了之后请求会打到网关的网页端，拿回一坨 HTML，
#    SDK 报的是 "'str' object has no attribute 'choices'" 这种跟配置毫无关系的解析错误。
# 2. 模型名以网关认的为准，不以域名为准。先看清单：
#    curl -H "Authorization: Bearer $KEY" https://your-gateway.example.com/v1/models
#    然后把 config.yaml 的 greeting.model 填成清单里的名字。
#OPENAI_BASE_URL=https://your-gateway.example.com/v1

# ============ Anthropic（config.yaml 里 provider: anthropic 时用这组）============
# SDK 按 ANTHROPIC_API_KEY → ANTHROPIC_AUTH_TOKEN 的顺序取凭据，
# 请求地址取 ANTHROPIC_BASE_URL（不设就是官方 api.anthropic.com）。

# —— 方式一：官方 API ——
#ANTHROPIC_API_KEY=sk-ant-your-key-here

# —— 方式二：走中转网关 / 自建代理 ——
# 网关一般发 Bearer token，用 AUTH_TOKEN 而不是 API_KEY；
# 两个都填时 SDK 只认 API_KEY，所以走网关就把上面那行注释掉。
#ANTHROPIC_BASE_URL=https://your-gateway.example.com
#ANTHROPIC_AUTH_TOKEN=your-gateway-token

# 注意：并非所有网关都能给本工具用。若探活报
#   503 ... this group only allows Claude Code clients
# 说明该网关只放行特定客户端，换官方 key 或换网关。

# ============ 代理 ============
# 这一条**跟着网关走，换网关必须重新确认**，两个方向的坑都踩过：
#
# - 网关是国内直连地址，而本机常开代理（Clash 之类）：走代理会把每次请求拖到超时，
#   整轮跑不动 → 要把它排除掉，取消下面这行的注释并填上域名。
# - 网关在墙外或只对代理出口放行：直连会被 Connection reset by peer 掐断，
#   这时**不能**把它放进 NO_PROXY，保持注释即可。
#
# 另：all_proxy=socks5://... 时 httpx 需要 socksio（已在依赖里），否则客户端都建不起来。
#NO_PROXY=your-gateway.example.com

# ============ 招呼语和 AI 判定可以用不同的模型 ============
# 判定外包/销售只回一个词，用快模型就行；招呼语要写文章，可以用好一点的。
# 在 config.yaml 的 ai_screen.model 里单独指定，留空则跟 greeting 走。
# 两者共用本文件这一套凭据——除非它们在不同的网关上，那就只能二选一。
